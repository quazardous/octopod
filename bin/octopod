#!/usr/bin/env bash
# Runs the TypeScript sources directly; tsx is loaded by absolute path so this works from
# any folder.
set -euo pipefail
ROOT="$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)"
exec node --import "file://$ROOT/node_modules/tsx/dist/esm/index.mjs" "$ROOT/src/cli.ts" "$@"
